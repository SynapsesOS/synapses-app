// Activity page — re-exports Analytics as the unified activity view.
// Agents + Analytics are merged here; the Analytics component already
// contains per-agent stats, tool usage, and the timeline.
export { Analytics as Activity } from "./Analytics";
